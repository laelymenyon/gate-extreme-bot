# Verified installed & imported on Python 3.14.4 (Linux) — 2026-08-09
# Pinned exactly. Regenerate with: .venv/bin/python -m pip freeze
aiohttp==3.14.3
websockets==17.0.1
numpy==2.5.2
pandas==3.0.5
PyYAML==6.0.3
python-dotenv==1.2.2
rich==15.0.0

# tests
pytest==9.1.1
pytest-asyncio==1.4.0

# transitive (pinned for reproducibility)
aiohappyeyeballs==2.7.1
aiosignal==1.4.0
attrs==26.1.0
frozenlist==1.8.0
idna==3.18
iniconfig==2.3.0
markdown-it-py==4.2.0
mdurl==0.1.2
multidict==6.7.1
packaging==26.3
pluggy==1.6.0
propcache==0.5.2
Pygments==2.20.0
python-dateutil==2.9.0.post0
six==1.17.0
yarl==1.24.5

# Deliberately NOT used:
#   TA-Lib      -> needs system C library; indicators implemented in numpy/pandas
#   ccxt        -> abstracts away Gate.io contract/margin semantics we must control
#   gate_api    -> official SDK is synchronous (urllib3); would block the asyncio
#                  loop. We reuse its exact signing algorithm in exchange/gate_client.py
