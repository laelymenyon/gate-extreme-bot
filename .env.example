# Copy to .env and fill in. .env is gitignored — never commit it.
GATE_API_KEY=
GATE_API_SECRET=

# Fail-closed master switch.
# Live orders require ALL THREE: DRY_RUN=false AND --mode live AND --confirm-live
DRY_RUN=true
